// Shared application constants
// Example usage: import { API_ENDPOINTS } from '@/constants'

export const API_ENDPOINTS = {
  // Define your API endpoints here
};

export const APP_CONFIG = {
  // Define app-wide configuration here
};
